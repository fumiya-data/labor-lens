{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop permissions for the LaborLens local UI shell.",
  "windows": ["main"],
  "permissions": ["opener:default"]
}
